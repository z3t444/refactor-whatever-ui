import DefaultLayout from "@/layouts/default";

export default function settings() {
  return (
    <DefaultLayout>
      <section>
        <div>
          <h1>Settings</h1>
        </div>
      </section>
    </DefaultLayout>
  );
}
